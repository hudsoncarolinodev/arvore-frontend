import { styled } from 'styled-components';

const MenuDropDownStyled = styled.div`
    display: flex;
    gap: 10px;
    img{
        width:24px;
        height: 24px;
        border-radius: 100%;
        cursor: pointer;
    }
`;


export  {MenuDropDownStyled}